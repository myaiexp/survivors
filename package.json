{
  "name": "survivors-roguelite",
  "version": "0.1.0",
  "description": "A dark medieval survivors-style roguelite",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
